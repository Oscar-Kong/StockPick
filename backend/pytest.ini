[pytest]
markers =
    integration: tests that may call external services (not run in default CI)
    optional: tests requiring optional dependencies
    slow: long-running tests
    no_db_isolation: skip autouse isolated_backend_env fixture
