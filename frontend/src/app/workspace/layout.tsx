// Workspace fills viewport below nav; research tab locks analyze pane height.
export default function WorkspaceLayout({ children }: { children: React.ReactNode }) {
  return <div className="workspace-layout">{children}</div>;
}
